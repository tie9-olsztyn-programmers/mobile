---
title: Lista i lokalny CRUD (JSON, SharedPreferences)
---

# Lista i lokalny CRUD (JSON, SharedPreferences)
- Zbudujemy aplikację *MiniKino* z osobistą listą filmów.
- Do nasze listy będzie można dodać nowe filmy.
- Aplikacja zapisze je w pliku lokalnym.


Utwórzmy nowy projekt `Empty Views Activity` *MiniKino*.

## Model danych

:::tip Co to jest Model danych?
- to klasa (najczęściej data class w Kotlinie), która
- ta klasa opisuje strukturę danych, z jakimi pracuje dana aplikacja.

:::

- utwórz folder/package w folderze, w którym jest `MainActiviry`
- w tym nowym folderze zrób plik `MovieEntry`
```kotlin
data class MovieEntry(
    val title: String,
    val genre: String,
    val rating: Int
)
```

## Projekt formularza dodawania filmu
W `activity_main.xml` definiujemy formularz dodawania nowego filmu do listy
```xml
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:id="@+id/main"
    android:orientation="vertical"
    >

        <EditText
            android:id="@+id/titleEditText"
            android:hint="Tytuł filmu"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <Spinner
            android:id="@+id/genreSpinner"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp" />

        <RatingBar
            android:id="@+id/ratingBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:numStars="5"
            android:stepSize="1.0"
            android:layout_marginTop="12dp" />

        <Button
            android:id="@+id/addButton"
            android:text="Dodaj film do listy"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp" />
    </LinearLayout>

```

## Obsługa formularza

### Włączenie bindingu

:::tip Czym jest ViewBinding?
To mechanizm Androida, który automatycznie generuje klasy dla każdego layoutu XML. Dzięki temu możemy:
- bezpiecznie odwoływać się do widoków przez ID (`binding.editTextTitle`)
- unikać `findViewById`
- mieć autouzupełnianie i sprawdzanie typów


:::

- w pliku `buld.gradle.kts` (Module) dodajemy:
```kts
// ...
    buildFeatures{
        viewBinding = true
    }
}

dependencies {
    implementation(libs.androidx.core.ktx)
    implementation //...
```
SYNCHRONIZUJEMY PROJEKT!


Zmieniamy `MainActivity.kt`
```kotlin
class MainActivity : AppCompatActivity() {

    private lateinit var binding: ActivityMainBinding

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()

        binding = ActivityMainBinding.inflate(layoutInflater)
        setContentView(binding.root)

        ViewCompat.setOnApplyWindowInsetsListener( //...

```

### `Spinner` z gatunkami filmów
W metodzie `onCreate`
```kotlin
val genres = listOf("Akcja", "Komedia", "Dramat", "Sci-Fi", "Fantasy", "Horror")

val genreAdapter = ArrayAdapter(this, android.R.layout.simple_spinner_item, genres)
genreAdapter.setDropDownViewResource(android.R.layout.simple_spinner_dropdown_item)

spinnerGenre.adapter = genreAdapter
```

### Obsługa kliknięcia
```kotlin

        binding.addButton.setOnClickListener {
            val title = binding.titleEditText.text.toString()
            val genre = binding.genreSpinner.selectedItem.toString()
            val rating = binding.ratingBar.rating.toInt()

            if (title.isNotBlank()) {
                val newMovie = MovieEntry(title, genre, rating)
                movieList.add(newMovie)

                Toast.makeText(
                    this,
                    "Dodano: $title",
                    Toast.LENGTH_SHORT).show()

                // Czyścimy formularz
                binding.titleEditText.text.clear()
                binding.ratingBar.rating = 0f
                binding.genreSpinner.setSelection(0)
            } else {
                Toast.makeText(this, "Podaj tytuł filmu", Toast.LENGTH_SHORT).show()
            }
        }
```

## Dodajemy `ListView`

### Definicja wyglądu
```xml
//...
 <ListView
            android:id="@+id/listViewMovies"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:dividerHeight="8dp" />
```

### Obsługa logiki działania
Metoda `onCreate`
```kotlin
//...
        genreAdapter.setDropDownViewResource(android.R.layout.simple_spinner_dropdown_item)
        binding.genreSpinner.adapter = genreAdapter

// logika listview ---------------------------
        val movieTitles = mutableListOf<String>()
        val listAdapter = ArrayAdapter(
            this,
            android.R.layout.simple_list_item_1,
            movieTitles
        )
        binding.moviesListView.adapter = listAdapter
// --------------------------------------------

        binding.addButton.setOnClickListener {
        //...
             if (title.isNotBlank()) {
                val newMovie = MovieEntry(title, genre, rating)
                movieList.add(newMovie)

                // --------------- nowy kod ---------
                movieTitles.add("${newMovie.title} - ${newMovie.genre} (*${newMovie.rating})")
                listAdapter.notifyDataSetChanged()
                //-----------------------------------

                Toast.makeText(
                    this,
                    "Dodano: $title",
                    Toast.LENGTH_SHORT).show()

                // Czyścimy formularz ...

```

## `kotlinx.serialization` - zapisywanie danych do pliku
:::note Czym jest *serialization* (serializacja)?
To proces zamiany obiektów programu na zapis tekstowy lub binarny, tak aby można je było:
- zapisać do pliku,
- przesłać przez sieć,
- odtworzyć później w tej samej lub innej aplikacji.

:::

:::tip *Deserialization*
to proces odtworzenia obiektów programu z zapisanego formatu


:::



### Dodajemy bibliotekę `serialization` do aplikacji
:::tip Może się zdarzyć błąd
- Dodajnie tej biblioteki może wywołać błąd.
- Jeżeli tak się stanie, po prostu dodaj do`[version]` wpis:
    - `kotlin `kotlin = "2.1.0"`

:::

W `.toml` dodajemy i synchronizujemy:
```toml
[versions]
kotlinxSerialization = "1.6.3"

[libraries]
kotlinx-serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "kotlinxSerialization" }

[plugins]
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }


```


W `build.gradle.kts` (module) dodajemy i synchronizujemy:
```kotlin
plugins {
    alias(libs.plugins.kotlin.serialization)

//...

dependencies {
    implementation(libs.kotlinx.serialization.json)

//...
```

- plugin = kompilator
- library = runtime


:::note Krótkie wyjaśnienie
1. Plik `.toml` (*Version Catalog*)
    - Analogia: Centralny spis inwentarza lub książka telefoniczna.
    - W dużych projektach masz setki plików. Gdybyś w każdym z nich wpisywał wersję biblioteki ręcznie (np. 1.2.3), a potem musiał ją zaktualizować, zginąłbyś w gąszczu poprawek.
    - Czym jest:
        - to plik konfiguracyjny (zazwyczaj `libs.versions.toml`),
        - plik tentrzyma wszystkie numery wersji i nazwy bibliotek w jednym miejscu.
    - Rola:
        - Zapewnia porządek.
        - Jeśli zmieniasz wersję biblioteki w `.toml`, zmienia się ona automatycznie w całej aplikacji

2. Plik build.gradle.kts
    - Analogia: Przepis kulinarny lub instrukcja montażu.
        - Sam spis części (`.toml`) nic nie zbuduje.
        - Musisz mieć instrukcję, która powie:
            - „Do tej konkretnej części aplikacji (modułu) dodaj te i te biblioteki”.
    - Czym jest:
        - To skrypt napisany w języku Kotlin (stąd końcówka `.kts` – Kotlin Script).
    - Rola:
        - Definiuje
            - parametry budowania (np.jaka jest minimalna wersja Androida, na której zadziała apka)
            - które elementy z katalogu `.toml` mają zostać użyte.

:::

Cecha | Libraries (Biblioteki) | Plugins (Wtyczki)
---   | ---                    |  ---
Główny cel | Dodanie nowych funkcji do Twojego kodu. | Dodanie nowych funkcji do procesu budowania.
Gdzie trafiają? | Są pakowane do pliku końcowego aplikacji. | Zostają tylko na komputerze programisty.
Pochodzenie | Inne firmy lub społeczność (Open Source).| "Twórcy narzędzi (Google, JetBrains)."
Sekcja w .toml | `[libraries]`  | [plugins]

### Zapis danych do pliku
#### Przycisk do zapisu danych
```xml
  <RatingBar
         ...

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="10dp">
        <Button
            android:id="@+id/addButton"
            android:text="Dodaj film do listy"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp" />

        <Button
            android:id="@+id/saveButton"
            android:text="Zapisz listę do JSON"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginLeft="10dp"
            />

    </LinearLayout>


        <ListView...
```

#### Uruchamianie zapisu
W `onCreate` dodajemy
```kotlin
  binding.saveButton.setOnClickListener{
            saveMoviesToJsonFile()
        }
```


#### Metoda zapisu danych

:::tip `Json`
- ma dwie główne funkcje:
    - `encodeToString`  bierze obiekt Kotlina i "wypycha" go do Stringa (JSON).
    - `decodeFromString` bierze Stringa (JSON) i "wciąga" go do obiektu Kotlina.

- może występować w dwóch wariantach:
    - domyślny `Json` tłumacz, który tłumaczy wszystko "jednym ciągiem",
        bez przerw, żeby było szybciej.
        - np. `val tekst = Json.encodeToString(mojeDane)`
    - unikalna instancja
        - np. taka, która będzie formatować tekst ` val json = Json { prettyPrint = true }`
        - np. ignoruje pola niezgodne z modelem `val json = Json {
      ignoreUnknownKeys = true // "Ignoruj klucze, których nie znam"
      }`
:::


:::danger `@Serializable`
Instancja `Json` potrafi serializować tylko te klasy, które mają adnotację `@Serializable`

:::tip Adnotacja `@...`
- W języku Kotlin adnotacje to metadane, czyli "dane o danych".
- Nie zmieniają one bezpośrednio logiki np. naszej klasy.
- Dają wskazówki kompilatorowi lub zewnętrznym bibliotekom, jak mają tę klasę obsługiwać.


:::


Dopiszmy adnotację do naszego modelu
```kotlin
@Serializable
data class MovieEntry(
    val title: String,
    val genre: String,
    val rating: Int
)
```




Do klasy `MainActivity` dodajemy nową metodę:
```kotlin
    private fun saveMoviesToJsonFile() {
        try {
            val json = Json { prettyPrint = true }
            val jsonString = json.encodeToString(movieList)

            val file = File(filesDir, "movies.json")
            // filesDir:
            // - obiekt typu File
            // - /data/data/<nazwa.pakietu>/files/
            // - jest właściwością dziedziczoną po klasie Context
            file.writeText(jsonString)


            Toast.makeText(
                this,
                "Zapisano ${movieList.size} filmów to JSON",
                Toast.LENGTH_SHORT
            ).show()

        } catch (e: Exception) {

            Toast.makeText(
                this,
                "Błąd zapisu pliku!",
                Toast.LENGTH_SHORT
            ).show()

            e.printStackTrace() // dokładne błędy w Logcat
        }

    }
```

#### Gdzie jest ten plik?
- W Android Studio kliknij *View* -> *Tool Windows* -> *Device File Explorer*
- `data > data > <nazwa_projektu> > files` tu będzie nasz plik `movies.json`


#### Odczyt danych z pliku
##### Cel
- odczytać plik `movies.json`
- zamienić JSON na `List<MovieEntry>`
- załadować dane do listy i `ListView` przy starcie aplikacji


##### Przenieśmy `movieTitles` z metody `onCreate` na poziom właściwości obiektu:
```kotlin

class MainActivity : AppCompatActivity() {

    private lateinit var binding: ActivityMainBinding

    private val movieList = mutableListOf<MovieEntry>()

    // tu przenieśliśmy deklarację i inicjalizację zmiennej movieTitles
    val movieTitles = mutableListOf<String>()


    lateinit var listAdapter: ArrayAdapter<String>
//....
```

Dzięki temu lista będzie dostępna w nowej metodzie, która będzie odczytywała dane z JSON.


:::tip Trzy różne role, trzy różne odpowiedzialności
- Model danych (np. `movieList`):
    - przechowuje prawdziwe dane aplikacji
    - odpowiada strukturze biznesowej (tytuł, gatunek, ocena)
    - jest zapisywany / odczytywany z JSON
    - UI go nie wyświetla bezpośrednio


- Lista dla UI (np. `movieTitles`):
    - zawiera tekstową reprezentację danych
    - jest przygotowana specjalnie do wyświetlania
    - powstaje na podstawie modelu danych
    - to ją obserwuje adapter


- Adapter (`ArrayAdapter`)
    - łączy listę UI z `ListView`
    - nie zna modelu danych
    - reaguje tylko na zmiany tej jednej listy, którą dostał
    - wymaga wywołania `notifyDataSetChanged()`
:::


###### Definiujemy metodę do odczytu danych
Do klasy `MainActivity` dodamy nową metodę\
```kotlin
private fun loadMoviesFromJsonFile() {
        try {
            val file = File(filesDir, "movies.json")
            if (!file.exists()) return


            val jsonString = file.readText()
            val json = Json { ignoreUnknownKeys = true }
            val loadedList = json.decodeFromString<List<MovieEntry>>(jsonString)

            movieList.clear()
            movieList.addAll(loadedList)


            // lista używana przez adapter
            movieTitles.clear()
            movieTitles.addAll(
                movieList.map {
                    "${it.title} (${it.genre}) ★${it.rating}"
                }
            )
            listAdapter.notifyDataSetChanged()

        } catch (e: java.lang.Exception) {
            Toast.makeText(
                this,
                "Błąd odczytu pliku!",
                Toast.LENGTH_SHORT
            ).show()
            e.printStackTrace()
        }
    }
```

##### Wywołujemy metodę `loadMoviesFromJsonFile`
W metodzie `onCreate()` na końcu wywołujemy metodę `loadMoviesFromJsonFile`




----

## SharedPreferences
Cel:
- w będziemy zapisywali w informację o tym, jaki gatunek filmu użytkownik ostatnio wybrał
- gdy aplikacja zostanie uruchomina ponowanie, domyślnie zaznaczony będzie ostatnio wybrany gatunek


:::note SharedPreferences
- najprostszy sposób na trwałe przechowywanie małych ilości danych.
- Wyobraź sobie to jako system "klucz-wartość" (jak słownik lub mapa),
- takie dane klucz-wartość zostają zapisane w pamięci telefonu.

:::

### Nowe właściwości w `MainActivity.kt`
```kotlin
class MainActivity : AppCompatActivity() {
/...

    private val PREFS_NAME = "minikino_prefs"
    private val KEY_LAST_GENRE = "last_genre"

    override fun onCreate(savedInstanceState: Bundle?) {//...
```
- `PREFS_NAME`
    - wartością jest nazwa pliku
    - plik zostanie stworzony, jeżeli go jeszcze nie ma
    - w pliku zostaną zapisane dane
- `KEY_LAST_GENRE`
    - w tej właściwości przechowujemy nazwę klucza
    - pod tym kluczem zapiszemy dane

### Zmianny w ` binding.addButton.setOnClickListener ` (dodawaniu do listy)
Gdy tworzony jest nowy film, zapamiętany zostanie jego gatunek
```kotlin
 binding.addButton.setOnClickListener {
            val title = binding.titleEditText.text.toString()
            val genre = binding.genreSpinner.selectedItem.toString()
            val rating = binding.ratingBar.rating.toInt()


            val prefs = getSharedPreferences(PREFS_NAME, MODE_PRIVATE)
            prefs.edit {
                putString(KEY_LAST_GENRE, genre)
            }
//...
```
`getSharedPreferences(PREFS_NAME, MODE_PRIVATE)`
- otwiera (lub tworzy) zestaw preferencji aplikacji o podanej nazwie
- i zwraca obiekt,
- przez który możemy zapisywać i odczytywać ustawienia.
    - `prefs.getString(...)`
    - `prefs.getInt(...)`
    - `prefs.getBoolean(...)`
    - `prefs.edit().putString(...)`

- `prefs.edit { putString(KEY_LAST_GENRE, genre) }`
    - otwiera edycję `SharedPreferences`,
    - zapisuje parę `klucz-> wartość` i automatycznie zapisuje zmiany.
    - `prefs.edit { ... }`
        - tworzy obiekt `SharedPreferences.Editor`
        - Kotlin przekazuje go do funkcji strzałkowej `{ ... }`
        - Kotlin po wyjściu z bloku automatycznie wywołuje `apply()`

- `putString(KEY_LAST_GENRE, genre)` zapisuje:
    - klucz: `"last_genre"`
    - wartość: np. `"Sci-Fi"`



### Gdzie znajdziemy plik z zapisanymi danymi?
```
/data
 └── data
     └── <nazwa twojej aplikacji>
         └── shared_prefs
             └── minikino_prefs.xml
```


### Wczytywanie danych przy starcie aplikacji
W metodzie `onCreate` dodajemy :
```kotlin
    val prefs = getSharedPreferences(PREFS_NAME, MODE_PRIVATE)
    val lastGenre = prefs.getString(KEY_LAST_GENRE, null)
    lastGenre?.let {
        val index = genres.indexOf(it)
        if (index >= 0) {
            binding.genreSpinner.setSelection(index)
        }
    }
```
- `val prefs = getSharedPreferences(PREFS_NAME, MODE_PRIVATE)`
    - jak poprzednio tworzymy obiekt do obsługi pliku
- `prefs.getString(KEY_LAST_GENRE, null)`:
    - odczytujemy dane z pliku przy użyciu klucza
- `genres` to lista stringów z nazwami rodzajów filmów
- `lastGenre?.let { ... }`
    - `?` dalszy kod wykona się, o ile `lastGenre` nie jest `null`
    - `let` wykona zawartość bloku, przekazując w zmiennej `it` wartość zmiennej `lastGenre`
-`genres.indexOf(it)`
    - zwraca indeks wartości zapisanej w `it`
    - u nas `it` to `lastGenre`, czyli string ostatnio wybranego rodzaju filmu



### Zapisujemy wartość `RatingBar`


#### W `MainActivity` dodajemy nową właściwość `KEY_LAST_RATING`:
```kotlin
//...
    private val PREFS_NAME = "minikino_prefs"
    private val KEY_LAST_GENRE = "last_genre"

    private val KEY_LAST_RATING = "last_rating"

//...
```

#### Zmiany podczas dodawania filmu - `putInt()`
```kotlin
   binding.addButton.setOnClickListener {
            val title = binding.titleEditText.text.toString()
            val genre = binding.genreSpinner.selectedItem.toString()
            val rating = binding.ratingBar.rating.toInt()

            val prefs = getSharedPreferences(PREFS_NAME, MODE_PRIVATE)
            prefs.edit {
                putString(KEY_LAST_GENRE, genre)
                putInt(KEY_LAST_RATING, rating)
            }
```

### Odczytujemy dane o ratingu
W metodzie `onCreate()` dodamy odczyt danych
```kotlin
//...
        val prefs = getSharedPreferences(PREFS_NAME, MODE_PRIVATE)
        val lastGenre = prefs.getString(KEY_LAST_GENRE, null)
        lastGenre?.let {
            val index = genres.indexOf(it)
            if (index >= 0) {
                binding.genreSpinner.setSelection(index)
            }
        }

// ----- nowy kod -----
        val lastRating = prefs.getInt(KEY_LAST_RATING, 0)
        binding.ratingBar.rating = lastRating.toFloat()

//....
```


---
## Usuwanie elementu z listy i z pamięci (`.json`)

Do metody `onCreate()` dodamy obsługę długiego kliknięcia.
To przytrzymane kliknięcie usunie element z listy i pamięci.
```kotln
 binding.moviesListView.setOnItemLongClickListener { _, _, position, _ ->
            // 1. Najpierw usuwamy dane z obu list
                movieList.removeAt(position)
                movieTitles.removeAt(position)

            // 2. Odświeżamy wygląd listy
            listAdapter.notifyDataSetChanged()

            // 3. Zapisujemy stan
            saveMoviesToJsonFile()

            Toast.makeText(
                this,
                "Usunięto element",
                Toast.LENGTH_SHORT
            ).show()

            true // musimy zwrócić true, aby poinformować, że obsłużaliśmy event kliknięcia
        }
```

## Aktualizacja danych

### Nowa aktywność do aktualizacji `EditMovieActivity`
`activity_edit_movie.xml`
```xml
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".EditMovieActivity"
    android:orientation="vertical"
    android:padding="36dp"
    android:gravity="center_horizontal|center_vertical"
    android:background="#C1F0F8"
    >
    <EditText
        android:id="@+id/titleUpdateEditText"
        android:hint="Tytuł filmu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Spinner
        android:id="@+id/genreUpdateSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp" />

    <RatingBar
        android:id="@+id/updateRatingBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:numStars="5"
        android:stepSize="1.0"
        android:layout_marginTop="12dp" />

    <Button
        android:id="@+id/saveChangesButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="#123123"
        android:layout_margin="20dp"
        android:text="Zapisz zmiany"/>
</LinearLayout>
```

### Wybór elementu przez klinknięcie
Aktualizacja włączy się, gdy użytkownik krótko kliknie na jeden z elementów listy.

#### Stworzenie *launchera*
`MainActivity.kt`
```kotlin

class MainActivity : AppCompatActivity() {

    private lateinit var binding: ActivityMainBinding

// ---------------------------------------------------------
    private val editMovieLauncher = registerForActivityResult(ActivityResultContracts.StartActivityForResult()) { result ->
        // jeżeli użytkownik kliknął w OK:
        if (result.resultCode == Activity.RESULT_OK) {
            val data = result.data
            val updatedMovie = if (Build.VERSION.SDK_INT >= 33) {
                //  to kod dla nowszych telefonów
                data?.getSerializableExtra("UPDATED_MOVIE", MovieEntry::class.java)
            } else {
                // to kod do wykonania w przypadku starszych wersji
                @Suppress("DEPRECATION")
                data?.getSerializableExtra("UPDATED_MOVIE") as? MovieEntry
            }
            val position = data?.getIntExtra("POSITION", -1) ?: -1

            if (updatedMovie != null && position != -1) {
                 // 1. Aktualizacja dla JSON-a
                movieList[position] = updatedMovie

                // 2. Aktualizacja "Wyglądu" (dla Adaptera)
                movieTitles[position] = updatedMovie.title

                // 3. Teraz Adapter zobaczy zmianę na liście stringów
                listAdapter.notifyDataSetChanged()

                // 4. Zapis do pliku
                saveMoviesToJsonFile()
            }
        }
    }

// ---------------------------------------------------------

    // właściwość przechowująca informacje o filmach
    private val movieList = mutableListOf<MovieEntry>()
    val movieTitles = mutableListOf<String>()
//....

```
:::tip editMovieLauncher
- to obiekt klasy `ActivityResultLayncher<Intent>`
- to pośrednik między naszym kodem a Androidem
- `<Intent>` aby ten *launcher*/wyrzutnia zadziałał, potrzebuje paliwa w postaci obiektu `Intent`
- ten obiekt przechowuje w sobie:
    - *kontrakt*/ wiedzę
        - jak wysłać żądanie
        - jak odebrać odpowiedź
    -*callback*, czyli funkcję `{result -> ...`
        - zostanie uruchomiana dopiero wtedy, gdy drugie "okno" zostanie zamknięte


To obiekt, z kanału komunikacji:
    - pozwala uruchomić inną aktywność
    - nasłuchuje, kiedy ta inna aktywność zakończy działanie, aby wykonać *callback*a

:::


#### Włączenie obsługi kliknięcia
```kotlin
binding.moviesListView.setOnItemClickListener { _, _, position, _ ->
            // wybrany element z listy movieList
            val selectedMovie = movieList[position]

            // tworzymy obiekt typu Intent
            val intent = Intent(this, EditMovieActivity::class.java)

            // dodajemy do obiektu intent informacje o wybranym elemencie
            // używamy klucza, aby drugie okno wiedziało, jak rozpakować dane
            intent.putExtra("EXTRA_MOVIE", selectedMovie) // patrz zmiana w modelu MovieEntry
            intent.putExtra("EXTRA_POSITION", position)


            // przełączamy się do nowej aktywności
            editMovieLauncher.launch(intent)
        }
```

Musimy jeszcze zaktualizować nasz model `MovieEntry`
```kotlin
@Serializable
data class MovieEntry(
    val title: String,
    val genre: String,
    val rating: Int
) : java.io.Serializable
```
- system Androida i język Kotlin to dwie różne warstwy
- Kotlin potrafi serializować `kotlinx.serialization` (`@Serializable`)
- Android nie wie, jak poradzić sobie z obiektem `MovieEntry`
    - nauczymy Androida przez dodanie `: java.io.Serializable`
    - teraz Android wraz z `Intent` potrafi przesyłać obiekt między aktywnościami

#### Odbieranie danych w drugiej Aktywności
##### Włączamy *binding*
```kotlin

class EditMovieActivity : AppCompatActivity() {

    lateinit var binding: ActivityEditMovieBinding


    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        binding = ActivityEditMovieBinding.inflate(layoutInflater)

        setContentView(binding.root)
        ViewCompat.setOnApplyWindowInsetsListener(
///....
```

##### Przenosimy listę gatunków do pliku zewnętrznego
- Mamy kłopot ze `Spinner`em, który korzysta z listy gatunków zdefiniowanej w `MainActivity`
- Lepiej mieć jedną listę z elementami, dostępną w różnych plikach, niż duplikować jedną listę w wielu plikach.

:::important
DON'T REPEAT YOURSELF

Nie powtarzaj się

:::


W Androidzie tą "Centralą Danych" jest plik `res/values/strings.xml`.

Dopiszmy do niego listę gatunków filmowych:
```xml
<resources>
    <string name="app_name">MiniKino</string>

    <string-array name="movie_genres">
        <item>Akcja</item>
        <item>Komedia</item>
        <item>Dramat</item>
        <item>Horror</item>
        <item>Sci-Fi</item>
        <item>Dokumentalny</item>
    </string-array>

</resources>
```

W `onCreate()` w głównej aktywności zmieniamy
```kotlin
// val genres = listOf("Akcja", "Komedia", "Dramat", "Sci-Fi", "Fantasy", "Horror")
// zamiast listy wpisanej na sztywno, dane "ściągniemy" z naszej centrali
   val genres = resources.getStringArray(R.array.movie_genres)

//...
```



W `onCreate()` `EditMovieActivity` (pobieramy dane do `Spinner`a i z `intent`:
```kotlin
    val genres = resources.getStringArray(R.array.movie_genres)

    val adapter = ArrayAdapter(
        this,
        android.R.layout.simple_spinner_item,
        genres)

    adapter.setDropDownViewResource(android.R.layout.simple_spinner_dropdown_item)
    binding.genreUpdateSpinner.adapter = adapter
```



##### Odbieramy dane
W metodzie `onCreate()` poniżej kodu z `Spinner`em
```kotlin
        // odbieramy dane
        val movieToEdit = if (Build.VERSION.SDK_INT >= 33) {
            intent.getSerializableExtra("EXTRA_MOVIE", MovieEntry::class.java)
        } else {
            @Suppress("DEPRECATION")
            intent.getSerializableExtra("EXTRA_MOVIE") as? MovieEntry
        }

        if(movieToEdit == null){
            finish()
            return
        }

        // wypełniamy formularz danymi
        binding.titleUpdateEditText.setText(movieToEdit.title)
        binding.updateRatingBar.rating = movieToEdit.rating.toFloat()
        val currentGenreIndex = adapter.getPosition(movieToEdit.genre)
        binding.genreUpdateSpinner.setSelection(currentGenreIndex)
```
:::tip `Build`
- `Build.VERSION.SDK_INT`
    - To jest "Wiek Telefonu" wyrażony liczbą.
    - Każdy telefon z Androidem ma ten numer zaszyty w sobie.
    - Np. Android 9 to numer 28. Android 13 to numer 33.
- `Build.VERSION_CODES.TIRAMISU`
    - To "ludzka nazwa" dla liczby 33.
    - Ciekawostka: Google nazywa wersje Androida od nazw słodyczy (alfabetycznie).
        - Oreo (Android 8)
        - Pie (Android 9)
        - ...
        - Tiramisu (Android 13)

:::

##### Zapisujemy dan
W metodzie `onCreate()` pisząc dalej:
```kotlin
  // zapisanie aktualizacji
        binding.saveChangesButton.setOnClickListener {

            val newTitle = binding.titleUpdateEditText.text.toString()
            val newGenre = binding.genreUpdateSpinner.selectedItem.toString()
            val newRating = binding.updateRatingBar.rating.toInt()

            if (newTitle.isNotEmpty()) {
                val updateMovie = MovieEntry(newTitle, newGenre, newRating)
                val returnIntent = Intent() // Konstruktor jest pusty, bo obiekt to pojemnik na dane
                returnIntent.putExtra("UPDATED_MOVIE", updateMovie)

                // pobiera pozycję z "paczki" która przyszła
                // ustawia tę samą pozycję w paczce , która wychodzi
                val originalPosition = intent.getIntExtra("EXTRA_POSITION", -1)
                returnIntent.putExtra("POSITION", originalPosition)

                //potwierdzenie, że operacja się udała
                setResult(RESULT_OK, returnIntent)

                //zniszczenie aktywności do aktualizacji
                finish()
            }else {
                Toast.makeText(
                    this,
                    "Tytuł jest wymagany",
                    Toast.LENGTH_SHORT).show()
            }

        }
```

| Kierunek Podróży| Nadawca (Kto wysyła?) | Odbiorca (Kto czyta?) | Nazwa Klucza (String) | Co jest w środku? |
| :--- | :--- | :--- | :--- | :--- |
| **Wylot (Start)** | `MainActivity` <br/> *(setOnItemClickListener)* | `EditMovieActivity` <br/> *(onCreate)* | **`"EXTRA_MOVIE"`** | Obiekt filmu (`MovieEntry`) do wyświetlenia w formularzu. |
| **Wylot (Start)** | `MainActivity` <br/> *(setOnItemClickListener)* | `EditMovieActivity` <br/> *(onCreate)* | **`"EXTRA_POSITION"`** | Numer wiersza (Int), który kliknął użytkownik. Musimy go zapamiętać! |
| | | | | |
| **Powrót (Koniec)** | `EditMovieActivity` <br/> *(przycisk Zapisz)* | `MainActivity` <br/> *(Launcher)* | **`"UPDATED_MOVIE"`** | Nowy, poprawiony obiekt filmu, który mamy zapisać. |
| **Powrót (Koniec)** | `EditMovieActivity` <br/> *(przycisk Zapisz)* | `MainActivity` <br/> *(Launcher)* | **`"POSITION"`** | Ten sam numer wiersza, który dostaliśmy na początku. Odsyłamy go, aby `MainActivity` wiedziało, który element podmienić. |



## Nazwy kluczy w oddzielnym pliku
Nazwy kluczy lepiej jest trzymać w oddzielnym pliku:
- zero literówek
- podpowiedzi IDE
- łatwa zmiana


Do głównego folderu aplikacji (tam gdzie jest `MainActivity.kt`) dodajemy nowy plik `Keys.kt`
```kotlin
object Keys {
    // Klucze do wysyłki (Tam ->)
    const val MOVIE_TO_EDIT = "EXTRA_MOVIE"
    const val POSITION_TO_EDIT = "EXTRA_POSITION"

    // Klucze do powrotu (<- Z powrotem)
    const val UPDATED_MOVIE = "UPDATED_MOVIE"
    const val RETURN_POSITION = "POSITION"
}
```

Teraz w kodzie podmieniamy odpowiednio:
- za string `"EXTRA_MOVIE"` -> `Keys.MOVIE_TO_EDIT`
- za string `"EXTRA_POSITION"` ->  `Keys.POSITION_TO_EDIT`
- ...





