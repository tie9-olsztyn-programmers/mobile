---
title: Future Car App
---


- Utworzymy galerię samochodów przyszłości.
- Wszystkie dane (też obrazki) będą pobierane z firestore.
- Zróbmy nowy projekt i uruchommy go.


## Włączmy binding

`build.gradle.kts` (Module)
```kts
android {
//...

  buildFeatures{
        viewBinding=true
    }


}

`MainActivity.kt`
```kotlin
: AppCompatActivity() {

    lateinit var binding: ActivityMainBinding

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()

        binding = ActivityMainBinding.inflate(layoutInflater)
        setContentView(binding.root)
//...
```

## Instalujemy potrzebne biblioteki

`lib.versions.toml`
```toml
[versions]
//...
firebaseBom = "34.10.0"
googleServices = "4.4.1"



[libraries]
firebase-bom = { group = "com.google.firebase", name = "firebase-bom", version.ref = "firebaseBom" }
firebase-firestore = { group = "com.google.firebase", name = "firebase-firestore" }
//...

[plugins]
//...
google-services = { id = "com.google.gms.google-services", version.ref = "googleServices" }



```


`build.gradle.kts` (Project)
```kts
plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.google.services) apply false //to dodajemy

}

```

`buld.gradle.kts` (Modul)

```kts
plugins {
    alias(libs.plugins.android.application)
    //    alias(libs.plugins.google.services) // dodajemy, ale na razie jest zakomentowane
}

//...


dependencies {
    implementation(platform(libs.firebase.bom))
    implementation(libs.firebase.firestore)
//...

```

- pamiętaj o synchronizacji projektu
- zbuduj projekt